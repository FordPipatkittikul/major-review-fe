# major-review-fe

# Requirement
- Node v20+ [LTS](https://nodejs.org/en/)

# setting up
- making sure you are in `client` directory and run `npm install`

# Getting start
- In `client` directory run `npm run dev`
